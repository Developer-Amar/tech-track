/**
 * Admin panel page — placeholder.
 * Phase 5: content management, registration/event controls,
 * participant management, communications, audit log, role management.
 */
export default function AdminPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-void">
      <p className="text-dormant font-mono text-sm">
        Admin panel — Phase 5
      </p>
    </main>
  );
}
