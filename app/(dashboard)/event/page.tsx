/**
 * Event gameplay page — placeholder.
 * Phase 4: riddle screen, code entry, coding question,
 * submission flow, Track progress component.
 */
export default function EventPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-void">
      <p className="text-dormant font-mono text-sm">
        Event gameplay — Phase 4
      </p>
    </main>
  );
}
