/**
 * Dashboard page — placeholder.
 * Phase 3: Lock Registration panel (active) + Tech Track Event panel (locked).
 * Phase 4: Event panel activates when admin flips event_live.
 */
export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-void">
      <p className="text-dormant font-mono text-sm">
        Dashboard — Phase 3
      </p>
    </main>
  );
}
